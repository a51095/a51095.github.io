{
  "name": "x-blog",
  "version": "0.0.1",
  "private": true,
  "hexo": {
    "version": "6.3.0"
  },
  "dependencies": {
    "hexo": "^7.3.0",
    "hexo-deployer-git": "^4.0.0",
    "hexo-generator-archive": "^2.0.0",
    "hexo-generator-category": "^2.0.0",
    "hexo-generator-index": "^3.0.0",
    "hexo-generator-tag": "^2.0.0",
    "hexo-renderer-ejs": "^2.0.0",
    "hexo-renderer-marked": "^6.0.0",
    "hexo-renderer-stylus": "^2.1.0",
    "hexo-server": "^3.0.0"
  },
  "scripts": {
    "clean": "hexo clean",
    "generate": "hexo generate",
    "server": "hexo server",
    "deploy": "hexo deploy",
    "debug": "npm run clean && npm run generate && npm run server",
    "build": "npm run clean && npm run generate && npm run deploy"
  }
}
